apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "janus-chart.fullname" . }}-test-connection"
  labels:
    {{- include "janus-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "janus-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
